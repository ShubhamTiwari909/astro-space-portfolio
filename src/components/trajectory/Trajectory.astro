---
/**
 * Trajectory — master plan §12.
 *
 * Role-only by decision (C1): no employer names. The position header is the
 * role plus a duration label, and `company`/dates are omitted entirely when
 * absent — never an empty label, never a "Company Name" placeholder.
 *
 * DOM order IS the chronology, so screen readers get the sequence for free
 * from nested ordered lists (§12.5). The spine is decorative and
 * `aria-hidden`; it renders fully drawn (scroll-draw is Phase 4).
 */
import type { CollectionEntry } from 'astro:content';
import MetricBlock from '../ui/MetricBlock.astro';
import Chip from '../ui/Chip.astro';

interface Props {
	positions: CollectionEntry<'experience'>[];
}

const { positions }: Props = Astro.props;
---

<ol class="m-0 grid list-none gap-16 p-0">
	{
		positions.map((position) => (
			<li>
				{/* Position header — role and duration only (§12.1). */}
				<header data-observe class="mb-10">
					<h3 class="text-display-m">{position.data.role}</h3>
					<p
						class="mt-1 font-mono text-label tracking-[0.14em] text-accent uppercase"
					>
						{position.data.durationLabel}
						{position.data.company && <span> · {position.data.company}</span>}
					</p>
					{position.data.summary && (
						<p class="mt-3 max-w-(--prose) text-ink-mid">{position.data.summary}</p>
					)}
				</header>

				{/* The spine: a hairline the burn events hang off. */}
				<ol
					class="relative m-0 grid list-none gap-8 border-s border-hairline p-0 ps-6 md:ps-10"
				>
					{position.data.achievements.map((event, i) => (
						<li data-observe class="relative">
							{/* Burn marker — pulsing beacon on the spine */}
							<span
								aria-hidden="true"
								class="pulse-glow absolute top-3 size-2 rounded-pill bg-accent max-md:-inset-s-7 md:-inset-s-11"
							/>

							<div class="glass-card p-5">
								<p
									class="mb-1 font-mono text-[0.625rem] tracking-[0.14em] text-ink-low uppercase"
								>
									Burn {String(i + 1).padStart(2, '0')}
								</p>
								<h4 class="mb-2 text-display-s text-ink-hi">{event.title}</h4>
								<p class="mb-0 max-w-(--prose) text-ink-mid">{event.description}</p>

								{event.metric && (
									<div class="mt-5">
										<MetricBlock
											value={event.metric.value}
											label={event.metric.label}
											basis={event.metric.basis}
										/>
									</div>
								)}

								{event.technologies && (
									<ul class="mt-4 flex list-none flex-wrap gap-2 p-0">
										{event.technologies.map((tech) => (
											<li>
												<Chip>{tech}</Chip>
											</li>
										))}
									</ul>
								)}
							</div>
						</li>
					))}
				</ol>
			</li>
		))
	}
</ol>
